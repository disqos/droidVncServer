<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.onaips.vnc"
    android:versionCode="58"
    android:versionName="1.1RC1" >

    <!-- Ajusta el nivel mínimo de SDK a algo más moderno -->
    <uses-sdk android:minSdkVersion="14" android:targetSdkVersion="28" />

    <!-- ✅ permisos realmente necesarios -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>

    <!-- ⚠️ deja este si tu binario usa "su" para arrancar -->
    <uses-permission android:name="android.permission.ACCESS_SUPERUSER"/>

    <!-- ❌ eliminados porque ya no hacen falta o son peligrosos:
         WRITE_SETTINGS, GET_TASKS, READ_PHONE_STATE, etc.
         Esos eran de APIs antiguas o usados para publicidad. -->

    <application
        android:name="MainApplication"
        android:debuggable="false" 
        android:icon="@drawable/icon" 
        android:label="droid VNC server" >

        <activity
            android:name=".MainActivity"
            android:icon="@drawable/icon"
            android:label="@string/app_name"
            android:launchMode="singleInstance">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>   

        <activity 
            android:name="preferences"
            android:label="Preferences" />


        <!-- Servicio principal que arranca el binario androidvncserver -->
        <service android:name=".ServerManager">
            <intent-filter>
                <action android:name="org.onaips.vnc.ServerManager"/>
            </intent-filter>
        </service>

        <!-- Autoarranque al boot -->
        <receiver android:name=".StartServiceAtBootReceiver" >
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <category android:name="android.intent.category.HOME" />
            </intent-filter>
        </receiver>

        <service android:name="avncservice"/>
    </application>
</manifest>
